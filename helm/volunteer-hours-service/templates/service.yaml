apiVersion: v1
kind: Service
metadata:
  name: {{ include "volunteer-hours-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "volunteer-hours-service.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080